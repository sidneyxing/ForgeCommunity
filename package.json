{
  "name": "forge-community",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "local": "vercel dev",
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "check": "node --check api/[...path].js && node --check api/data.js && node scripts/check-static.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0"
  },
  "devDependencies": {
    "vercel": "^48.10.10"
  }
}